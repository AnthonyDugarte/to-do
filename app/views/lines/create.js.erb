lines = document.querySelector("[data-model=\"<%= Project.name %>\"][data-id=\"<%= @project&.id %>\"] [data-container=\"<%= Line.name %>\"]") || document.querySelector("[data-container=\"<%= Line.name %>\"]")

tmp_el = document.createElement("template")
tmp_el.innerHTML = "<%= j render(@line, project: @project) %>"

lines.appendChild(tmp_el.content.firstElementChild)

MicroModal.close("<%= model_modal_id Line.new %>")