line_modal = document.getElementById("modal-container")

<% content_for :modal_title do %>
    <h1>Editing Project</h1>
<% end %>

line_modal.innerHTML = "<%= j render(layout: "shared/modal", locals: { resource: @project }, &-> { render 'form', project: @project, remote: true }) %>"

<% # TODO: move this to a helper %>
MicroModal.show("<%= model_modal_id @project %>", microModalDefaultConfig)