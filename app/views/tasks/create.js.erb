tasks = document.querySelector("[data-model=\"<%= Line.name %>\"][data-id=\"<%= @line&.id %>\"] [data-container=\"<%= Task.name %>\"]") || document.querySelector("[data-container=\"<%= Task.name %>\"]")

tmp_el = document.createElement("template")
tmp_el.innerHTML = "<%= j render(@task, line: @line, lines: @lines, project: @project) %>"

tasks.appendChild(tmp_el.content.firstElementChild)

MicroModal.close("<%= model_modal_id Task.new %>")