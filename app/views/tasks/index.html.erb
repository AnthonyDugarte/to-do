<p id="notice"><%= notice %></p>

<h1>Tasks</h1>

<div data-container="<%= Task.name %>">
    <%= render @tasks, line: @line %>
</div>

<br>

<%= link_to 'New Task', [:new, @line, :task], remote: true %>
